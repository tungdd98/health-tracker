# Shared AI Support Assets

This directory is the single source of truth for repository-local AI support assets.

Rules:

- Edit shared skills only under `.ai/skills/`.
- `.agents/skills` and `.claude/skills` are compatibility symlinks.
- Keep `AGENTS.md` and `CLAUDE.md` separate because they target different tools.
