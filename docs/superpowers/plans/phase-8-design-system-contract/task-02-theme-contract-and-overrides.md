# Task 02: Extend the theme contract and shared overrides

**Files:**

- Modify: `libs/theme/src/lib/theme.ts`

- [x] Add semantic palette roles for shared surfaces, border strengths, and warning/status usage.
- [x] Add typed theme helpers for radius, shadow, and compact typography roles used repeatedly across screens.
- [x] Align MUI component overrides so buttons, cards, dialogs, drawers, inputs, chips, and bottom navigation inherit the same contract.

**Expected outcome:** Screens can consume semantic theme roles instead of duplicating raw style literals.
